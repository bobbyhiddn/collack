# Build outputs
dist/
.node_cache/
.love_cache/

# Capacitor generated artifacts (regenerated from ios-template/ on every build)
capacitor/node_modules/
capacitor/dist/
capacitor/ios/
# (capacitor/ios-template/ is source-controlled; capacitor/ios/ is the
#  cap-sync working copy that build-ios.sh reseeds on every run.)

# OS / editor cruft
.DS_Store
*.swp
*~
.vscode/
.idea/

# Lua build artifacts (none expected, but)
*.luac

# CI / lockfiles we don't want to source-control yet
package-lock.json
capacitor/package-lock.json
